/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
/.idea
/.vscode
/.vagrant
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
.env
.phpunit.result.cache
/resources/views/emails/plantillas
/bootstrap/cache/services.php
/bootstrap/cache/packages.php
/bootstrap/cache/config.php
/public/convertidor/*

# cache files
.php-cs-fixer.cache
_ide_helper.php
.phpstorm.meta.php
.DS_Store
public/.DS_Store
.htaccess

# ============================================
# ARCHIVOS SENSIBLES - NUNCA SUBIR A GITHUB
# ============================================

# Configuración de servidores (contiene credenciales)
config/servers.json

# Variables de entorno adicionales
.env.local
.env.production
.env.staging
.env.*.local

# Claves SSH y certificados
*.pem
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk

# Archivos de credenciales
credentials.json
secrets.json
auth.json

# Logs de deployment
logs/deploy*.log
deployment.log

# ============================================
# DOCKER MULTIEMPRESA
# ============================================

# .env por cliente (BD, APP_KEY, password) — NUNCA al repo
/clientes/*/.env
/clientes/*/.env.*

# Credenciales del stack infra (MYSQL_ROOT_PASSWORD)
/infra.env

# Dumps de BD de clientes (grandes y con datos)
/dumps
